schema_version: 2
goal_id: 2026-05-23-goal-relations-cli-verification
status: done

proof_step:
  from: "Goal Relations protocol is planned as contract metadata, but there is no CLI surface to inspect or validate cross-pack links."
  target_delta: "The CLI can list relations, check relation evidence, and render thread-derived graph views over Goal Packs."
  proof_path:
    - "Add failing CLI tests for relation enum validation, predecessor existence, evidence record_ref lookup, evidence token lookup, related_to warning behavior, thread filtering, and graph rendering."
    - "Implement goal-proof relations list/check/graph over parsed Goal Packs and evidence records."
    - "Document the relations command family in root, package, and skill docs."
    - "Dogfood relations check and graph rendering against the goal-relations thread."
  checks:
    - "bun run check"
    - "goal-proof check docs/goal-proof/goals/2026-05-23-goal-relations-cli-verification"
    - "goal-proof relations check docs/goal-proof/goals --thread goal-relations"
    - "goal-proof relations graph docs/goal-proof/goals --thread goal-relations --json"
  failure_inspection:
    - packages/cli/src/goal-proof.ts
    - packages/cli/src/lib/goal-pack.ts
    - packages/cli/test/goal-pack-cli.test.ts
    - README.md
    - README.zh-CN.md
    - packages/cli/README.md
    - packages/cli/README.zh-CN.md
    - skills/goal/goal-proof-system/SKILL.md

active_work_item: null

work_items:
  - id: W001
    type: implementation
    status: done
    objective: "Implement relation parsing, validation, thread filtering, and relations list/check/graph CLI commands."
    allowed_scope:
      - packages/cli/src/**
      - packages/cli/test/**
      - README.md
      - README.zh-CN.md
      - packages/cli/README.md
      - packages/cli/README.zh-CN.md
      - skills/**
      - docs/**
    checks:
      - "bun run check"
      - "goal-proof check docs/goal-proof/goals/2026-05-23-goal-relations-cli-verification"
      - "goal-proof relations check docs/goal-proof/goals --thread goal-relations"
      - "goal-proof relations graph docs/goal-proof/goals --thread goal-relations --json"
    stop_if:
      - "The implementation requires evidence record schema changes, stored graph state, a thread registry, or automatic successor creation."
      - "Existing CLI command behavior would need a breaking change."
      - "Required predecessor evidence cannot be verified from evidence record JSON."
  - id: W999
    type: review
    status: done
    objective: "Audit the relations CLI verification surface against the completion standard."
    allowed_scope:
      - docs/goal-proof/goals/2026-05-23-goal-relations-cli-verification/**
    checks:
      - "goal-proof check docs/goal-proof/goals/2026-05-23-goal-relations-cli-verification"
    stop_if:
      - "Any completion-standard token lacks evidence-backed evidence."

blockers:

last_check:
  result: pass
  checks:
    - "bun run check [pass]"
    - "goal-proof-dev check docs/goal-proof/goals/2026-05-23-goal-relations-cli-verification [pass]"
    - "goal-proof-dev relations check docs/goal-proof/goals --thread goal-relations --json [pass]"
    - "goal-proof-dev relations graph docs/goal-proof/goals --thread goal-relations --json [pass]"

next_action: done
