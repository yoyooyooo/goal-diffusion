# 2026-05-24-goal-proof-v2-dogfood-migration Implementation Plan

## Goal Pack

- goal: `docs/goal-proof/goals/2026-05-24-goal-proof-v2-dogfood-migration/goal.yaml`
- progress: `docs/goal-proof/goals/2026-05-24-goal-proof-v2-dogfood-migration/progress.yaml`
- evidence records: `docs/goal-proof/goals/2026-05-24-goal-proof-v2-dogfood-migration/evidence.jsonl`
- plan: `docs/goal-proof/goals/2026-05-24-goal-proof-v2-dogfood-migration/plans/<work_id>.md`
- work item: `W002`

本计划只约束高风险 schema / CLI / 历史 Goal Pack 迁移 slice。执行状态继续由本 Goal Pack 的 `progress.yaml` 和 `evidence.jsonl` 拥有；`docs/roadmap/**` 只能承载迁移波次摘要和证据链接。

## Protected Boundary

- objective: migrate this repository to the v2 goal/progress/evidence schema and strong-agent optimistic workflow.
- completion: docs, skills, templates, CLI behavior, checker behavior, tests, and dogfood Goal Packs use `goal.yaml`, `completion`, `checks`, and `claim_evidence` as the primary path.
- claim_limit: repository-local migration only; no external project migration, package release, or stable public v2 release claim.
- stop_if: public compatibility or release/versioning needs a product decision; historical rewrite would destroy unrecoverable evidence; default path would require strict proof fields.

## Allowed Scope

- W003: `skills/goal/goal-proof-system/templates/**`, `skills/goal/goal-proof-system/references/**`, `skills/goal/goal-proof-system/SKILL.md`, phase skills.
- W004: `packages/cli/**`, `scripts/**`, root/package README files, docs touched by CLI behavior.
- W005: `docs/goal-proof/goals/**`, `docs/goal-proof/README.md`, `docs/goal-proof/sources/**`.
- W999: current Goal Pack files plus `docs/goal-proof/README.md`.

## Execution Chunks

1. Template and checker-facing examples:
   - Rename the active template from legacy contract shape to `goal.yaml`.
   - Convert evidence record examples from `commands` to `checks`.
   - Convert completion review examples to `claim_evidence`, `not_claimed`, and `remaining_gaps`.
   - Keep relation metadata in `goal.yaml`.

2. CLI parser/checker/renderer/tests:
   - Teach Goal Pack loading to prefer `goal.yaml`.
   - Render `completion.signal` and `completion.required_evidence`.
   - Validate implementation evidence records with `checks` and completion review `claim_evidence`.
   - Keep compatibility only as an internal migration aid if needed, not as a product promise.
   - Update tests before claiming checker validation.

3. Historical dogfood Goal Packs:
   - Rewrite each `docs/goal-proof/goals/**/contract.yaml` to `goal.yaml`.
   - Convert old fields: `completion_review` -> `completion`, `architecture_standard` -> `engineering_guidance`, `agent_authority_policy.protected_fields` -> `agent_authority.requires_human_decision`, evidence record `commands` -> `checks`.
   - Preserve evidence meaning while rewriting schema; do not delete historical evidence record claims.
   - Update `docs/goal-proof/README.md` indexes and thread notes.

4. Completion review:
   - Run repository check, docs review, v2 Goal Pack check, and static vocabulary scan.
   - Append E999 review evidence record with `claim_evidence`, `not_claimed`, `remaining_gaps`, and `completion_satisfied: true` only if every completion proof element has current evidence.

## Verification

- `bun run check`
- `python3 skills/governance/docs-governance/scripts/run_docs_audit.py --repo .`
- `goal-proof check docs/goal-proof/goals/2026-05-24-goal-proof-v2-dogfood-migration`
- `rg -n "contract.yaml|completion_review|architecture_standard|protected_fields|\"commands\"|Goal Plan" README.md README.zh-CN.md AGENTS.md docs skills packages scripts`

## Evidence Record Requirements

Each implementation evidence record must include:

```json
{
  "schema_version": 2,
  "evidence_id": "E###",
  "work_id": "W###",
  "type": "implementation",
  "result": "done",
  "changed_files": [],
  "checks": [{ "kind": "command", "cmd": "<verification>", "status": "pass" }],
  "evidence": [],
  "claims": [],
  "not_claimed": [],
  "summary": "",
  "next_action": "continue"
}
```

E999 completion review must map every `completion.required_evidence` clause to evidence:

```json
{
  "schema_version": 2,
  "evidence_id": "E999",
  "work_id": "W999",
  "type": "review",
  "result": "done",
  "decision": "complete",
  "completion_satisfied": true,
  "claim_evidence": [],
  "not_claimed": [],
  "remaining_gaps": [],
  "summary": "",
  "next_action": "done"
}
```

## Handoff

- ready_for_run: true
- blocked_by: []
- next_action: continue
