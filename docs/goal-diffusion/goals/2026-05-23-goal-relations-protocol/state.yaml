version: 1
goal_id: 2026-05-23-goal-relations-protocol
status: done

current_edge:
  from: "Goal Diffusion currently connects work through per-pack state and receipts, but cross-pack follow-up relationships are only informal."
  target_delta: "Goal Relations become a documented, minimal protocol that supports thread continuity without creating a second planning system."
  harnessed_path:
    - "Update the controller skill and phase skills with Goal Thread, Goal Relation, derived graph view, and done-pack closure rules."
    - "Update templates so new Goal Packs can carry goal_relations.thread_id and goal_relations.links."
    - "Update README docs with the user-facing concept model and command roadmap."
    - "Use local lexical scan plus memdex semantic detection to prove retired bundled-goal vocabulary is absent from current guidance."
  verify:
    - "bun run check"
    - "goal-diffusion check docs/goal-diffusion/goals/2026-05-23-goal-relations-protocol"
    - "rg -n <retired-bundled-goal-vocabulary-pattern> README.md README.zh-CN.md docs skills packages"
    - "memdex ask --repo . --json <retired-vocabulary-absence-query>"
  failure_inspection:
    - README.md
    - README.zh-CN.md
    - skills/goal-diffusion/SKILL.md
    - skills/goal-diffusion/references/checker-rules.md
    - skills/goal-diffusion/templates/contract.yaml
    - skills/goal-diffusion/templates/receipt.jsonl
    - skills/goal-plans/SKILL.md
    - skills/finding-harnessed-path/SKILL.md
    - skills/diffusion-implementation/SKILL.md

active_task: null

tasks:
  - id: T001
    type: worker
    status: done
    objective: "Document Goal Relations in skills, references, templates, and README files without adding CLI behavior."
    allowed_scope:
      - README.md
      - README.zh-CN.md
      - docs/**
      - skills/**
    verify:
      - "bun run check"
      - "goal-diffusion check docs/goal-diffusion/goals/2026-05-23-goal-relations-protocol"
      - "rg -n <retired-bundled-goal-vocabulary-pattern> README.md README.zh-CN.md docs skills packages"
      - "memdex ask --repo . --json <retired-vocabulary-absence-query>"
    stop_if:
      - "The protocol requires a thread-owned lifecycle, thread-owned state, nested Goal Packs, or a stored graph artifact."
      - "The current docs must retain retired bundled-goal vocabulary as a present concept."
      - "The protocol cannot explain successor relationships through predecessor receipt evidence."
  - id: T999
    type: audit
    status: done
    objective: "Audit the Goal Relations protocol against its completion oracle."
    allowed_scope:
      - docs/goal-diffusion/goals/2026-05-23-goal-relations-protocol/**
    verify:
      - "goal-diffusion check docs/goal-diffusion/goals/2026-05-23-goal-relations-protocol"
    stop_if:
      - "Any oracle token lacks receipt-backed evidence."

blockers:

last_verification:
  result: pass
  commands:
    - "goal-diffusion-dev relations check docs/goal-diffusion/goals --thread goal-relations --json [pass]"

next_decision: done
